#include<bits/stdc++.h>
#include<stack>
using namespace std;

int main()
{
	int x;
	scanf("%d",&x);

	for(int i=0;i<x;i++)
	{
		string str;
		scanf("%s",&str);
		stack<char> st;
		st.push(str[0]);
		int j=1;

		while(st.empty()!=true)
		{
			char ch=st.top();
			if(ch)
		}
		
	}

}